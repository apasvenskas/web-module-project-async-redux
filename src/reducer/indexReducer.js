import React from "react";

const initialState = {
    toBeDetermined: [],
    error: ''
};

const reducer = (state = initialState, action) => {
    switch(action.type) {
    default:
        return(state);
    }
}

export default reducer;